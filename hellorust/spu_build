
rust_features=""

if [ $SPU_CONFIG_joe ] ; then
    rust_features="$rust_features joe"
fi

cd hellorust
if [ $rust_features ] ; then
    cargo build --features $rust_features
else
    cargo build
fi

mkdir -p $SPU_INSTALL_DIR/usr/bin
cp target/debug/hellorust $SPU_INSTALL_DIR/usr/bin
